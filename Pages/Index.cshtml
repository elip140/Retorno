@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Retorno</h1>
    @*<form method="post" id="2" >
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="submit" value="Teste" class="btn btn-primary" />
            </div>        
    </form>*@
    <div class="row justify-content-center text-center">
        <h2 class="display-3"> Camera ID: 3 (8084)</h2>
    </div>
    @(Model.response)
    <form asp-page-handler="cam" method="POST" class="row row-cols-auto justify-content-center text-center">
        <input type="submit" value="Enviar todos da Camera 3" class="btn btn-primary" />
    @foreach (var reg in Model.CList)
    {
        <form method="post" class="card col m-2">
            <p>RecNo = @(reg.RecNo)</p>
            <p>@(reg.GetId())</p> 
            <button type="submit" asp-page-handler="SendUnit" asp-route-id="@reg.GetId()" class="btn btn-primary">Enviar</button>
        </form>
    }
    </form>
</div>
